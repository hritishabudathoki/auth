export const metadata = {
  title: "Settings — Admin — ExploreEase",
};

export default function AdminSettingsPage() {
  return (
    <div className="flex-1 bg-canvas p-8">
      <div>
        <p className="text-[11px] uppercase tracking-[1.5px] text-zinc-500">
          Admin
        </p>
        <h1 className="mt-1 text-3xl font-bold uppercase tracking-[1px] text-white">
          Settings
        </h1>
      </div>
    </div>
  );
}
